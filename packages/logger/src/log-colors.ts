import colors from 'ansi-colors'

const logColors = {
  debug: colors.dim,
  error: colors.red,
  info: colors.blue,
  log: colors.reset,
  success: colors.green,
  warning: colors.yellow
}

export default logColors
